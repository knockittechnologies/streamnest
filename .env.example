# PostgreSQL connection string
DATABASE_URL="postgresql://streamnest:streamnest@localhost:5432/streamnest?schema=public"

# Random 32+ char secret used to sign auth session tokens
JWT_SECRET="replace-this-with-a-long-random-string"
